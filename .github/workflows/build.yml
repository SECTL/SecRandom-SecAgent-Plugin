name: Build plugin

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Build SRPX
        run: dotnet build SecRandom.SecAgentPlugin.csproj -c Release -p:CreateSrpx=true -p:UseLocalPluginSdk=false -p:BuildInParallel=false -p:UseSharedCompilation=false
      - name: Upload SRPX
        uses: actions/upload-artifact@v4
        with:
          name: secrandom-secagent-plugin
          path: srpx/*.srpx
